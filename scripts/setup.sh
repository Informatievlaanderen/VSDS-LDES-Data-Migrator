#!bin/bash
#
# Placeholder script for any setup needed before running the Data Migrator.
# You can volume mount the scripts directory to override this script and to add any other scripts that can be invoked from this script.
#
# e.g.:
# NODE_OPTIONS=--max_old_space_size=5000
